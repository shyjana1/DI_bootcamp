let initState = {
    input: '',
    items: [],
}

export const reducer = (state=initState,action = {})=> {
    switch (action,type) {
        case value:
            
            break;
    
        default:
            break;
    }

}